experiment:
  id: "A1"
  name: "CNN Baseline"
  description: "Reference CNN baseline — flat 12-class"
  seeds: [42]

defaults:
  data: default
  model: cnn_baseline
  train: default

train:
  max_epochs: 80
  lr: 5.0e-4
