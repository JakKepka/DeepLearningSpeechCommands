experiment:
  id: "A5"
  name: "AST fine-tuning"
  description: "Pretrained Audio Spectrogram Transformer fine-tuned for 12-class"
  seeds: [42]

defaults:
  data: default
  model: ast
  train: default

train:
  lr: 1.0e-4
  max_epochs: 60
  batch_size: 64
