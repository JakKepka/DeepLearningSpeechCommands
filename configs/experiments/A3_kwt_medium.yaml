experiment:
  id: "A3"
  name: "KWT-medium"
  description: "Keyword Transformer (medium) — flat 12-class"
  seeds: [42]

defaults:
  data: default
  model: kwt_medium
  train: default

train:
  max_epochs: 80
  lr: 2.0e-4
