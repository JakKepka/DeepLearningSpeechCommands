experiment:
  id: "C3"
  name: "Hierarchical KWT-medium"
  description: "Hierarchical pipeline using A3 KWT-medium as stage model"
  seeds: [42]

defaults:
  data: default
  model: kwt_medium
  train: default

train:
  hierarchical: true
  max_epochs: 80
  lr: 2.0e-4
