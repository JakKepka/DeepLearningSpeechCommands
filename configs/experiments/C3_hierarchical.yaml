experiment:
  id: "C3"
  name: "Hierarchical pipeline"
  description: "Two-stage hierarchical classifier: (silence|target|unknown) → 10 commands"
  seeds: [42, 123, 456]

defaults:
  data: default
  model: kwt_small
  train: default

train:
  hierarchical: true
