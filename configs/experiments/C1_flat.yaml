experiment:
  id: "C1"
  name: "Hierarchical CNN baseline"
  description: "A1 CNN baseline in hierarchical mode"
  seeds: [42]

defaults:
  data: default
  model: cnn_baseline
  train: default

train:
  hierarchical: true
  max_epochs: 80
  lr: 5.0e-4
