experiment:
  id: "C1"
  name: "Flat 12-class baseline"
  description: "KWT-small flat 12-class, no rebalancing — baseline for silence/unknown study"
  seeds: [42, 123, 456]

defaults:
  data: default
  model: kwt_small
  train: default
