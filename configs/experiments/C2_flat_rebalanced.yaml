experiment:
  id: "C2"
  name: "Flat 12-class + rebalancing"
  description: "KWT-small flat 12-class with weighted sampler for silence/unknown"
  seeds: [42, 123, 456]

defaults:
  data: default
  model: kwt_small
  train: default

train:
  rebalance: true
