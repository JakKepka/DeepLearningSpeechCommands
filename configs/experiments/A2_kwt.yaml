experiment:
  id: "A2"
  name: "KWT-2"
  description: "Keyword Transformer KWT-2 — flat 12-class"
  seeds: [42]

defaults:
  data: default
  model: kwt_2
  train: default

data:
  n_mels: 40

train:
  max_epochs: 80
  lr: 3.0e-4
